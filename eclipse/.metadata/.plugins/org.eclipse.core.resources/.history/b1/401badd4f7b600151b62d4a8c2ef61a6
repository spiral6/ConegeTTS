package conege.spiral6.conegetts;

import net.minecraft.command.ICommand;
import net.minecraftforge.event.CommandEvent;

public class TTSCommand implements ICommand{
		@ForgeSubscribe
		public void CommandStuff(CommandEvent e){
			
			if (e.isCancelable()){
				e.setCanceled(true);
			}
		}
		
		minecraftforge.EVENT_BUS.register(new TTSCommand());
		
		
		


}
